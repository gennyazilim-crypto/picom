# Installer art previews

PNG exports of the NSIS bitmaps for review (same pixel sizes):

- `installer-header.png` — 150×57
- `installer-sidebar.png` — 164×314

Source of truth remains the `.bmp` files under `assets/installer/windows/`.
Regenerate with `npm run installer:windows:art`, then re-export previews if needed.
