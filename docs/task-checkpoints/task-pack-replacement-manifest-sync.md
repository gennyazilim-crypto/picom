# Task pack replacement manifest sync

This checkpoint records the remaining manifest files from `electron_supabase_livekit_all_old_new_tasks_001_473_txt.zip`.

## Status

Complete.

## What was applied

- Added the replacement patch README into the tracked task documentation folder.
- Added the `TASKS_TO_REPLACE_INDEX.txt` manifest.
- Added the `tasks_replacement_metadata.json` manifest.
- Verified the replacement manifest contains 150 replacement tasks and that the addendum `task_127_ADDENDUM_first_run_onboarding.txt` is present.

## Scope note

The 001-473 task implementation sequence was already completed through task 473. This checkpoint preserves the remaining package metadata so the completed work remains traceable to the source ZIP.

## Validation

- `npm run tasks:replacement-manifest:smoke`
