# Task 058 checkpoint - Overlay/context menu AppIcon usage

## Completed

- Routed overlay close/search icons through the semantic icon map.
- Kept context menu text-only to avoid visual churn.
- Preserved `AppIcon` currentColor behavior and overlay accessibility labels.

## Verification

- Run `npm run typecheck`.
- Run `npm run build`.
- Manually open Settings, ImagePreview, and Command Palette.