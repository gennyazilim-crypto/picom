# Task 073 checkpoint

- Confirmed `VITE_DATA_SOURCE=mock` is documented in `.env.example`.
- Added `isMockMode` and `isSupabaseMode` helpers in app config.
- Documented mock vs Supabase mode behavior for MVP development.
- Kept runtime behavior defaulting safely to mock mode.
