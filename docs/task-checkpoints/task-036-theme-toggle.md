# Task 036 checkpoint - ThemeToggle and persistence placeholder

## Completed

- Added reusable `ThemeToggle` component.
- Wired the WindowTitleBar theme control through `ThemeToggle`.
- Added token-based toggle styling for light and dark mode.
- Documented local theme persistence and future Supabase sync path.

## Verification

- Run `npm run typecheck`.
- Run `npm run build`.
- Launch `npm run dev` and click the title bar theme toggle.
- Confirm the selected theme persists after refresh/relaunch.