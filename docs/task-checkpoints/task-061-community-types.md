# Task 061 checkpoint - TypeScript community domain types

## Completed

- Expanded `src/types/community.ts` into explicit MVP domain types.
- Added typed ID aliases and role/status/channel unions.
- Kept existing mock data compatible.
- Avoided backend-only or secret fields.

## Verification

- Run `npm run typecheck`.
- Run `npm run build`.