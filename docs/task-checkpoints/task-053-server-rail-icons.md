# Task 053 checkpoint - ServerRail AppIcon usage

## Completed

- Routed ServerRail utility icons through the semantic icon map.
- Preserved `AppIcon` rendering and currentColor behavior.
- Preserved brand logo/community mock marks separately from UI icons.

## Verification

- Run `npm run typecheck`.
- Run `npm run build`.
- Manually inspect add, discover, and settings rail buttons in dev mode.