# Task 072 checkpoint

- Added typed fallback role, member, channel, and community in MVP app state.
- Active community fallback now handles missing ids safely.
- Active channel fallback now handles missing ids safely.
- Channel selection is wrapped so unknown channel ids do not break the shell.
