PICOM ROOT OWNER DASHBOARD — CLAUDE CODE TASK PACKAGE

Purpose:
Build a real-data, realtime, analytics-driven dashboard capable of managing the full Picom platform.

Root owner:
f.tayboga@gmail.com

Critical security rule:
The email address is only the bootstrap identity. Root authorization must be confirmed server-side by authenticated user_id, root-role persistence, RLS and permission checks. Never protect the dashboard only with a frontend email comparison.

Execution order:
01 → 40

Each TXT file is a separate Claude Code task.
Run one task at a time.
Review every generated report before continuing.
Mock-only, placeholder-only and smoke-only completion is forbidden.

The package includes:
- owner bootstrap and Panel sidebar button
- complete RBAC
- support team authorization
- advertising team authorization
- security unit authorization
- moderation/trust & safety
- real analytics and realtime metrics
- full platform operations
- immutable audit logs
- MFA and privileged-action controls
- hosted multi-role acceptance
- final production readiness
