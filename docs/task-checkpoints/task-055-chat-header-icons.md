# Task 055 checkpoint - ChatHeader AppIcon usage

## Completed

- Routed ChatHeader action icons through the semantic icon map.
- Routed the channel type icon through the approved text/voice semantic icons.
- Preserved `AppIcon` currentColor behavior and button `aria-label` values.

## Verification

- Run `npm run typecheck`.
- Run `npm run build`.
- Manually inspect ChatHeader icons in text and voice channels.