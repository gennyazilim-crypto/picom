PICOM ROOT DASHBOARD UI — CURSOR TASK PACKAGE

Purpose:
Design and implement the complete owner-only Picom management dashboard interface in Cursor.

Execution order:
01 → 40

Use together with the Claude Code backend/root-dashboard task package.
Claude tasks define the real permissions, data, APIs, RLS and realtime behavior.
Cursor tasks define the production UI, workflows, components and visual consistency.

Important:
- Root owner bootstrap account: f.tayboga@gmail.com
- Panel visibility alone is not security.
- Never use hardcoded fake metrics as final output.
- Do not redesign Picom's unrelated product surfaces.
- Run one TXT task at a time.
