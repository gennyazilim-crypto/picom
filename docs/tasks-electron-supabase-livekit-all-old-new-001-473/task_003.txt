TASK 003: Full scope definition: Electron desktop + Supabase + LiveKit + production roadmap
PHASE: A. Safety, scope, recovery
PRIORITY: P0

GLOBAL RULES FOR EVERY TASK
- This is an Electron desktop app for Windows, Linux, and macOS.
- Do not create mobile UI or web-first responsive UI.
- The UI must follow the uploaded reference image quality: premium desktop chat layout, soft rounded surfaces, compact spacing, clean light/dark modes.
- Use Coolicons as the approved icon system; do not use Discord branding, Discord logo, copied Discord assets, or exact Discord colors.
- Use Supabase for the MVP backend: Auth, Postgres, RLS, Storage, Realtime, and Edge Functions where appropriate.
- Use LiveKit/WebRTC for MVP voice chat and screen sharing.
- Do exactly one task at a time.
- Do not refactor unrelated working code.
- Do not delete previous work unless explicitly instructed.
- After each task: run the smallest relevant test, list changed files, explain how to test, then commit.

PHASE GUIDANCE
Focus on project safety, repository discipline, scope clarity, and preventing another loss of work. Prefer documentation and read-only inspection unless the task explicitly asks for setup.

OBJECTIVE
Complete this task: Full scope definition: Electron desktop + Supabase + LiveKit + production roadmap.

REQUIREMENTS
- Use Supabase Auth/Postgres/RLS/Storage/Realtime primitives where appropriate.
- Never bypass RLS for client-side access; privileged actions must use Edge Functions only when justified.
- Document required Supabase SQL, policies, environment variables, and test steps.
- Keep Electron native APIs outside React components; use preload and typed service wrappers.
- Maintain contextIsolation and avoid nodeIntegration in the renderer unless explicitly justified.
- Verify the app still runs in Electron dev mode after the task.
- Use LiveKit for room connection/token flow where implementation is needed.
- Protect LiveKit tokens; generate them server-side or through Supabase Edge Functions.
- Document Windows/Linux/macOS audio and screen share permission differences.
- Prefer documentation/checklists/runbooks unless implementation is explicitly required.
- Separate staging, beta, and production assumptions.
- Include rollback, verification, and known-risk sections.

IMPLEMENTATION NOTES
- Keep changes scoped to this task.
- If the task is documentation-only, create/update the relevant docs file and do not change runtime code.
- If implementation needs a decision, document the decision before coding.
- If a dependency is required, explain why and avoid unnecessary large dependencies.
- If the current project structure differs, adapt safely and document the chosen path.
- If something cannot be completed safely, mark it as TODO with a clear reason instead of guessing.

ACCEPTANCE CRITERIA
- Changed files are listed.
- Relevant test/manual verification steps are documented.
- No unrelated features were added.
- Existing MVP UI remains stable unless this task explicitly changes it.
- Supabase configuration or SQL is documented.
- RLS/security implications are documented.
- Voice/screen-share behavior is tested or documented per platform.
- No secrets are committed.

AFTER FINISHING
- List changed files.
- List commands run.
- Explain how to test.
- Commit with a message like: task-003 Full scope definition: Electron desktop + Supabase.
