# ServerRail icon mapping

Task 053 confirms ServerRail UI icons are rendered through `AppIcon` and backed by the MVP semantic icon map.

## Mapped icons

- Add community: `mvpUiIconMap.serverRail.addCommunity`
- Discover communities: `mvpUiIconMap.serverRail.discover`
- Settings: `mvpUiIconMap.serverRail.settings`

## Notes

- The Picom home/logo mark remains a brand asset, not a Coolicons replacement.
- Community stack items use mock community marks from data, not UI icon-set assets.
- Icon-only buttons keep `aria-label` values.