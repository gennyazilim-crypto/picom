# Legacy provenance exception: 20260808220000

Production has an applied migration-history version `20260808220000`. Read-only inspection of `supabase_migrations.schema_migrations` found `name`, `statements`, `rollback`, `created_by`, and `idempotency_key` all null for that exact row.

Official `supabase migration fetch --linked` therefore generated `20260808220000_.sql` containing only `;`. That is a history representation, not recoverable historical SQL, and is not copied into the canonical migration directory.

Multiple Git candidates exist for the same version, but production metadata cannot tie either candidate to the applied record. Reconstructing or choosing one would manufacture provenance. This document is the sole exact-version exception; any new unexplained remote-only version remains release-blocking.

Observed production history fingerprint: `E907595D903DE48DFB43B7681757588044E8CD2E10C1555C15C1F41FAB5BCF02`.
