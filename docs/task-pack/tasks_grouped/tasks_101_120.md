# Codex Tasks 101–120

# Codex Task 101 — Account Deletion Workflow

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Post-launch operational and community management foundations

## Goal

Prepare safe soft/scheduled account deletion flow with warnings and confirmation.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare safe soft/scheduled account deletion flow with warnings and confirmation.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 102 — Data Export Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Post-launch operational and community management foundations

## Goal

Prepare user data export job/status/download placeholders without sensitive fields.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare user data export job/status/download placeholders without sensitive fields.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 103 — Community Ownership Transfer

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Post-launch operational and community management foundations

## Goal

Add owner-only transfer flow, modal confirmation and leave-owner safety.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Add owner-only transfer flow, modal confirmation and leave-owner safety.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 104 — Community Delete Safety

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Post-launch operational and community management foundations

## Goal

Harden community deletion with owner check, name confirmation and soft-delete placeholder.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Harden community deletion with owner check, name confirmation and soft-delete placeholder.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 105 — Private Channel Permission UI

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Post-launch operational and community management foundations

## Goal

Prepare role/member permission UI for private channel view/send/manage access.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare role/member permission UI for private channel view/send/manage access.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 106 — Channel Category Management

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Post-launch operational and community management foundations

## Goal

Add category create/edit/delete/reorder endpoints and sidebar actions.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Add category create/edit/delete/reorder endpoints and sidebar actions.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 107 — Channel Reorder UI Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Post-launch operational and community management foundations

## Goal

Add admin reorder mode with move up/down/category controls or drag placeholder.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Add admin reorder mode with move up/down/category controls or drag placeholder.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 108 — Message Moderation Filters Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Post-launch operational and community management foundations

## Goal

Prepare blocked words/spam/mentions/link blocking settings and send validation.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare blocked words/spam/mentions/link blocking settings and send validation.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 109 — User Blocking Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Post-launch operational and community management foundations

## Goal

Prepare blocked users routes/settings/profile actions and collapsed message behavior.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare blocked users routes/settings/profile actions and collapsed message behavior.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 110 — Final Post-Launch Roadmap

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Post-launch operational and community management foundations

## Goal

Create roadmap phases for stabilization, beta, realtime scaling, voice, DM/friends, discovery, moderation and enterprise.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Create roadmap phases for stabilization, beta, realtime scaling, voice, DM/friends, discovery, moderation and enterprise.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 111 — Bot System Foundation

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Prepare bot users, bot management, community installation and BOT badges.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare bot users, bot management, community installation and BOT badges.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 112 — Webhook Foundation

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Prepare channel webhooks, secure tokens, webhook messages and settings UI.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare channel webhooks, secure tokens, webhook messages and settings UI.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 113 — Slash Commands Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Add composer slash command suggestions and basic built-in command placeholders.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Add composer slash command suggestions and basic built-in command placeholders.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 114 — Custom Emoji Foundation

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Prepare community emoji upload/list/delete and EmojiPicker custom emoji support.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare community emoji upload/list/delete and EmojiPicker custom emoji support.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 115 — Stickers Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Prepare sticker picker, community sticker settings and safe placeholder sticker assets.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare sticker picker, community sticker settings and safe placeholder sticker assets.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 116 — Polls Feature

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Add poll models/routes/create modal and poll message voting UI.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Add poll models/routes/create modal and poll message voting UI.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 117 — Events / Scheduled Sessions Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Prepare community events, RSVP placeholders and event reminders.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare community events, RSVP placeholders and event reminders.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 118 — Threads Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Prepare message threads, ThreadPanel/View and thread message routes.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare message threads, ThreadPanel/View and thread message routes.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 119 — Forum Channel Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Add forum channel type, post list view and create post placeholder.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Add forum channel type, post list view and create post placeholder.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 120 — Announcement Channel Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Prepare announcement channel flag, restricted posting and special styling.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare announcement channel flag, restricted posting and special styling.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

