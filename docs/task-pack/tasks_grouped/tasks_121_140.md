# Codex Tasks 121–140

# Codex Task 121 — Message Bookmarks / Saved Messages

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Allow save/unsave messages and Saved Messages view with jump placeholder.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Allow save/unsave messages and Saved Messages view with jump placeholder.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 122 — Message Drafts Per Channel

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Persist per-channel/DM composer drafts and show draft indicator.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Persist per-channel/DM composer drafts and show draft indicator.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 123 — Per-Channel Composer Permissions

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Show clear composer disabled states for read-only/private/announcement/muted/slow/offline.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Show clear composer disabled states for read-only/private/announcement/muted/slow/offline.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 124 — Slow Mode Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Add channel slowModeSeconds, backend enforcement and composer countdown.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Add channel slowModeSeconds, backend enforcement and composer countdown.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 125 — Message Search Result Jump

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Navigate to search result message, load context placeholder and highlight target.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Navigate to search result message, load context placeholder and highlight target.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 126 — Advanced Notification Routing

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Centralize notification decision logic for active channel, DND, muted prefs, mentions and inbox.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Centralize notification decision logic for active channel, DND, muted prefs, mentions and inbox.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 127 — Multi-Account Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Prepare account switcher metadata and modal without storing passwords.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare account switcher metadata and modal without storing passwords.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 128 — Community Export / Import Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Prepare safe config export/import excluding messages, personal data and secrets.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare safe config export/import excluding messages, personal data and secrets.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 129 — Plugin System Architecture Document

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Document safe future plugin system, sandboxing, permissions and risks; no dynamic code loading.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Document safe future plugin system, sandboxing, permissions and risks; no dynamic code loading.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 130 — Bot API Architecture Document

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Document bot auth, token safety, permissions, events, actions, rate limits and versioning.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Document bot auth, token safety, permissions, events, actions, rate limits and versioning.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 131 — Developer Portal Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Prepare development-only portal for bots, webhooks, applications and docs placeholders.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare development-only portal for bots, webhooks, applications and docs placeholders.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 132 — Community Discovery Production Plan

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Document public discovery requirements, moderation, categories, ranking and privacy risks.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Document public discovery requirements, moderation, categories, ranking and privacy risks.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 133 — Analytics Privacy-Friendly Placeholder

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Create analyticsService with non-sensitive event placeholders and opt-in setting.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Create analyticsService with non-sensitive event placeholders and opt-in setting.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 134 — Data Migration Strategy Document

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Document Prisma migration workflow, staging/production rollout, backup and rollback limitations.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Document Prisma migration workflow, staging/production rollout, backup and rollback limitations.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 135 — Final Advanced Feature Audit

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Advanced community/platform features

## Goal

Audit advanced feature placeholders and ensure they do not break MVP flows.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Audit advanced feature placeholders and ensure they do not break MVP flows.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 136 — Feature Flags Foundation

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Scalability, compatibility, desktop native and enterprise planning

## Goal

Create typed featureFlagService with local/env/default flags and dev viewer.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Create typed featureFlagService with local/env/default flags and dev viewer.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 137 — Remote Config Foundation

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Scalability, compatibility, desktop native and enterprise planning

## Goal

Add /config/client and frontend remoteConfigService with cache/default fallback.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Add /config/client and frontend remoteConfigService with cache/default fallback.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 138 — Client / Server Version Compatibility

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Scalability, compatibility, desktop native and enterprise planning

## Goal

Add minimum/recommended client version checks and update required/banner screens.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Add minimum/recommended client version checks and update required/banner screens.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 139 — Realtime Horizontal Scaling Preparation

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Scalability, compatibility, desktop native and enterprise planning

## Goal

Prepare Redis adapter/pubsub strategy and docs for multi-instance realtime/presence.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Prepare Redis adapter/pubsub strategy and docs for multi-instance realtime/presence.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

# Codex Task 140 — Database Performance Audit

## Global guardrails

Remember: this is a **Windows/Linux desktop-only community chat app**.

- Do **not** create mobile UI.
- Do **not** create web-first responsive UI.
- Do **not** use Discord branding, logos, copied assets, or exact Discord colors.
- Keep the existing MVP UI with Coolicons stable unless the task explicitly says otherwise.
- Do only this task.
- Do not refactor unrelated working code.
- Do not add unrelated advanced roadmap features.
- After finishing, list changed files and explain how to test.

## Phase

Scalability, compatibility, desktop native and enterprise planning

## Goal

Review indexes/query patterns for messages, read state, members, roles, search, notifications and audit logs.

## Requirements

- Implement or document only the scope described by this task.
- Preserve existing working MVP flows unless the task is specifically a QA/fix task.
- Keep mock mode working when relevant.
- Keep API mode working when relevant.
- Use typed TypeScript boundaries where code is added.
- Use existing services/stores/components where possible.
- If a dependency, native API, backend service, or production credential is unavailable, add a safe placeholder and document the gap instead of breaking the app.
- If this task is documentation-only, do not change application behavior.

## Acceptance criteria

- The task goal is satisfied or the remaining gap is documented clearly.
- No mobile UI is introduced.
- No Discord branding/assets/exact colors are introduced.
- Existing desktop layout remains stable.
- Build/typecheck issues introduced by this task are fixed or listed as known blockers.
- Changed files and test steps are reported.

## Suggested Codex instruction

```text
Review indexes/query patterns for messages, read state, members, roles, search, notifications and audit logs.

Work within the existing project structure. Do not delete existing work. If a file conflict appears, explain it before overwriting.
```


---

