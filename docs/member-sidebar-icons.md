# MemberSidebar icon mapping

Task 057 confirms MemberSidebar icons are rendered through `AppIcon` and backed by the MVP semantic icon map.

## Mapped icons

- Participant search: `memberSidebar.search`
- Profile semantic key is reserved in the registry for future profile-specific UI affordances.

## Accessibility

The search input has visible placeholder text and the icon is decorative through `AppIcon`.