# Payout Reconciliation

Issues table: `publisher_payout_reconciliation_issues`

Codes: MISSING_PROVIDER_RECORD | MISSING_LOCAL_RECORD | AMOUNT_MISMATCH | CURRENCY_MISMATCH | STATUS_MISMATCH | DUPLICATE_PROVIDER_REF

No automatic economic fix. Quarantine for finance review + audit `PAYOUT_RECONCILIATION_MISMATCH`.

Provider runtime absent → reconciliation contract exists; runtime `NOT_RUN`.
