# Publisher Finance Statements

`publisher_finance_statements` derived from immutable ledger via `service_finalize_publisher_finance_statement`.

Per currency. No FX. Not a tax invoice unless legally verified.

Publisher reads FINALIZED via `get_my_publisher_finance_statements`.
