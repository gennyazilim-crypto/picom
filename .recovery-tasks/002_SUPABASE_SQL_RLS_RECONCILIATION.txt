TASK 002 - SUPABASE SQL, MIGRATIONS, RLS, STORAGE

PROJECT: C:\Users\ACER\Desktop\picom
Read 000_READ_FIRST.txt first.
Preserve unrelated user, Claude, and Cursor changes. Never reset or clean the worktree.
Do not redesign UI, add mobile UI, expose secrets, or commit .env files.
Supabase mode must never silently fall back to mock data.
Use service/repository layers; React components must not call Supabase directly.
Do not disable RLS, tests, security gates, or error reporting.
Use forward-only migrations and deploy required Edge Functions to the verified hosted project.
Complete means implementation, local tests, hosted evidence, focused commit, and push.
Report root cause, changed files, commands, deployments, results, risks, commit, and push.

Compare repository migrations/types with hosted migration history and OpenAPI.
Reconcile profiles, communities, memberships, roles, categories, channels, messages, attachments, reactions, mentions, reads, follows, friendships, blocks, DMs, Feed, stories, notifications, meetings, rate limits, audits, buckets, indexes, triggers, RPCs, and policies.
Use new idempotent forward migrations. Never rewrite hosted history or disable RLS.
Add positive/negative RLS tests. Non-participants cannot read DMs; visitors cannot read private channels.
Align generated database types, apply to verified hosted project, and query the resulting schema.
Acceptance: no missing table/column/RPC errors; buckets and migrations align; two-user RLS tests pass.
