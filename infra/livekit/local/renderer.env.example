# Public renderer values for local self-hosted LiveKit development only.
# Copy these two values into the developer's ignored .env.local when running
# Picom against the local provider. Never add API keys or API secrets here.
VITE_LIVEKIT_ENABLED=true
VITE_LIVEKIT_URL=ws://127.0.0.1:17880
