import { mockStickers } from "../data/mockStickers";
import { AppIcon } from "./AppIcon";
export function StickerPicker({ onSelect, onClose }: { onSelect: (stickerId: string) => void; onClose: () => void }) { return <div className="sticker-picker" role="dialog" aria-label="Choose a Picom sticker"><header><div><strong>Picom stickers</strong><span>Original local placeholders</span></div><button className="icon-button" type="button" aria-label="Close stickers" onClick={onClose}><AppIcon name="close" size="sm" /></button></header><div>{mockStickers.map((sticker) => <button key={sticker.id} type="button" className={`sticker-option tone-${sticker.tone}`} onClick={() => onSelect(sticker.id)}><span><AppIcon name="image" size="lg" /></span><strong>{sticker.title}</strong></button>)}</div></div>; }
