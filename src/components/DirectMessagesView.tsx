import { useEffect, useMemo, useRef, useState, type FormEvent } from "react";
import "./DirectMessagesView.css";
import type { DirectConversation, DirectMessage, DirectMessageAttachment } from "../types/directMessages";
import type { Attachment } from "../types/community";
import { dateTimeService } from "../services/dateTimeService";
import { messageDraftService } from "../services/messageDraftService";
import { AppIcon } from "./AppIcon";
import { VerifiedAvatarFrame } from "./VerifiedAvatarFrame";
import { VerifiedBadge } from "./VerifiedBadge";
import { getUserVerificationSummary } from "../utils/verificationHelpers";
import { ImagePreviewModal } from "./ImagePreviewModal";

type DirectMessagesViewProps = { conversations: DirectConversation[]; activeConversationId: string; currentUserId: string; friendRequestCount: number; onSelectConversation: (conversationId: string) => void; onBackToCommunity: () => void; onOpenFriends: () => void; onOpenPendingFriends: () => void; onOpenProfile: (userId: string) => void; onSendMessage: (conversationId: string, body: string) => Promise<boolean>; onSetMuted: (conversationId: string, muted: boolean) => Promise<boolean>; onArchive: (conversationId: string) => Promise<boolean> };
type ConversationListProps = Pick<DirectMessagesViewProps, "conversations" | "activeConversationId" | "onSelectConversation">;
type DirectMessagesSidebarProps = ConversationListProps & Pick<DirectMessagesViewProps, "friendRequestCount" | "onOpenFriends" | "onOpenPendingFriends">;

function DirectAvatar({ conversation, large = false, compact = false }: { conversation: DirectConversation; large?: boolean; compact?: boolean }) {
  const verification = getUserVerificationSummary(conversation.participantUserId);
  return <span className={`direct-avatar-shell status-${conversation.participantStatus} ${large ? "large" : ""} ${compact ? "compact" : ""}`}><VerifiedAvatarFrame userId={conversation.participantUserId} label={conversation.participantName} avatarUrl={conversation.participantAvatarUrl} size="medium" avatarSize={large ? 64 : compact ? 38 : 42} verification={verification} /><i className="direct-status-dot" role="img" aria-label={`${conversation.participantName} is ${conversation.participantStatus}`} /></span>;
}

export function DirectConversationItem({ conversation, active, onSelect }: { conversation: DirectConversation; active: boolean; onSelect: () => void }) {
  const verification = getUserVerificationSummary(conversation.participantUserId);
  return <button className={`direct-conversation ${active ? "active" : ""}`} onClick={onSelect} aria-current={active ? "page" : undefined}><DirectAvatar conversation={conversation} /><span className="direct-copy"><strong><span>{conversation.participantName}</span><VerifiedBadge verification={verification} size="xs" /></strong><small>{conversation.lastMessagePreview || "Start a conversation"}</small></span><span className="direct-conversation-meta"><time dateTime={conversation.updatedAt}>{dateTimeService.formatMessageTime(conversation.updatedAt)}</time>{conversation.unreadCount ? <em aria-label={`${conversation.unreadCount} unread messages`}>{conversation.unreadCount}</em> : messageDraftService.hasDraft({ directConversationId: conversation.id }) ? <i className="direct-draft-indicator">Draft</i> : null}</span></button>;
}

export function DirectConversationList({ conversations, activeConversationId, onSelectConversation }: ConversationListProps) {
  if (!conversations.length) return <div className="direct-list-empty"><AppIcon name="inbox" size="xl" /><strong>No conversations found</strong><span>Try another name or start a message from a profile.</span></div>;
  return <div className="direct-conversation-list" aria-label="Recent direct conversations">{conversations.map((conversation) => <DirectConversationItem key={conversation.id} conversation={conversation} active={conversation.id === activeConversationId} onSelect={() => onSelectConversation(conversation.id)} />)}</div>;
}

export function DirectMessagesSidebar(props: DirectMessagesSidebarProps) {
  const [query, setQuery] = useState("");
  const [notice, setNotice] = useState<string | null>(null);
  const filtered = useMemo(() => { const normalized = query.trim().toLocaleLowerCase(); return normalized ? props.conversations.filter((conversation) => `${conversation.participantName} ${conversation.participantUsername}`.toLocaleLowerCase().includes(normalized)) : props.conversations; }, [props.conversations, query]);
  return <aside className="direct-list"><header className="direct-list-header"><div><span className="eyebrow">Private</span><h2>Messages</h2></div><button className="icon-button" type="button" aria-label="Start a new direct message" title="Start a new direct message" onClick={() => setNotice("Open a profile and choose Message to start a conversation.")}><AppIcon name="plus" size="sm" /></button><label className="direct-search"><AppIcon name="search" size="sm" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search conversations" aria-label="Search direct conversations" /></label></header><nav className="direct-shortcuts" aria-label="Direct message sections"><button type="button" onClick={props.onOpenFriends}><AppIcon name="users" size="sm" /><span>Friends</span></button><button type="button" onClick={props.onOpenPendingFriends}><AppIcon name="inbox" size="sm" /><span>Pending requests</span><small>{props.friendRequestCount}</small></button></nav><div className="direct-list-title"><strong>Direct Messages</strong><span>{filtered.length}</span></div>{notice ? <button className="direct-sidebar-notice" type="button" onClick={() => setNotice(null)}>{notice}<AppIcon name="close" size="xs" /></button> : null}<DirectConversationList conversations={filtered} activeConversationId={props.activeConversationId} onSelectConversation={props.onSelectConversation} /></aside>;
}

export function DirectChatHeader({ conversation, searchOpen, searchQuery, detailsOpen, onSearchOpen, onSearchChange, onDetailsToggle, onBack, onOpenProfile }: { conversation: DirectConversation; searchOpen: boolean; searchQuery: string; detailsOpen: boolean; onSearchOpen: () => void; onSearchChange: (value: string) => void; onDetailsToggle: () => void; onBack: () => void; onOpenProfile: (userId: string) => void }) {
  const verification = getUserVerificationSummary(conversation.participantUserId);
  return <header className="direct-chat-header"><div className="direct-chat-header-inner"><button className="icon-button direct-back-button" aria-label="Back to community chat" title="Back to community chat" onClick={onBack}><AppIcon name="chevronRight" size="sm" /></button><button className="direct-chat-identity" onClick={() => onOpenProfile(conversation.participantUserId)}><DirectAvatar conversation={conversation} /><span><strong><span>{conversation.participantName}</span><VerifiedBadge verification={verification} size="sm" /></strong><small>@{conversation.participantUsername} / {conversation.participantStatusText}</small></span></button>{searchOpen ? <label className="direct-header-search"><AppIcon name="search" size="sm" /><input autoFocus value={searchQuery} onChange={(event) => onSearchChange(event.target.value)} placeholder="Search this conversation" aria-label="Search this conversation" /></label> : null}<div className="direct-header-actions"><button className="icon-button" type="button" aria-label="Search conversation" title="Search conversation" onClick={onSearchOpen}><AppIcon name={searchOpen ? "close" : "search"} size="sm" /></button><button className={`icon-button ${detailsOpen ? "active" : ""}`} type="button" aria-label="Toggle conversation details and shared media" aria-pressed={detailsOpen} title="Conversation details" onClick={onDetailsToggle}><AppIcon name="image" size="sm" /></button><button className="icon-button" type="button" aria-label="More direct message options" title="More options"><AppIcon name="more" size="sm" /></button></div></div></header>;
}

function toPreviewAttachment(attachment: DirectMessageAttachment): Attachment {
  return { id: attachment.id, type: "image", url: attachment.url, mimeType: attachment.mimeType, alt: attachment.name, width: attachment.width, height: attachment.height, scanStatus: "skipped_development" };
}

function DirectAttachmentGrid({ attachments, onPreview }: { attachments: readonly DirectMessageAttachment[]; onPreview: (attachment: Attachment) => void }) {
  return <div className={`direct-attachment-grid count-${Math.min(attachments.length, 4)}`}>{attachments.slice(0, 4).map((attachment) => attachment.type === "image" ? <button key={attachment.id} type="button" title={attachment.name} aria-label={`Preview ${attachment.name}`} onClick={() => onPreview(toPreviewAttachment(attachment))}><img src={attachment.url} alt={attachment.name} /></button> : <a key={attachment.id} href={attachment.url} download={attachment.name}><AppIcon name="paperclip" size="sm" />{attachment.name}</a>)}</div>;
}

type DirectMessageGroupData = { id: string; authorId: string; messages: DirectMessage[] };
const directMessageGroupWindowMs = 5 * 60 * 1000;

function groupDirectMessages(messages: readonly DirectMessage[]): DirectMessageGroupData[] {
  const groups: DirectMessageGroupData[] = [];
  for (const message of messages) {
    const current = groups[groups.length - 1];
    const previous = current?.messages[current.messages.length - 1];
    const timeGap = previous ? new Date(message.createdAt).getTime() - new Date(previous.createdAt).getTime() : Number.POSITIVE_INFINITY;
    const contextBreak = Boolean(previous?.replyPreview || message.replyPreview || previous?.deletedAt || message.deletedAt);
    if (current && previous && current.authorId === message.authorId && timeGap >= 0 && timeGap <= directMessageGroupWindowMs && !contextBreak) current.messages.push(message);
    else groups.push({ id: message.id, authorId: message.authorId, messages: [message] });
  }
  return groups;
}

export function DirectMessageGroup({ group, conversation, currentUserId, onPreview }: { group: DirectMessageGroupData; conversation: DirectConversation; currentUserId: string; onPreview: (attachment: Attachment) => void }) {
  const own = group.authorId === currentUserId;
  const finalMessage = group.messages[group.messages.length - 1];
  const verification = getUserVerificationSummary(conversation.participantUserId);
  return <article className={`direct-message-group ${own ? "own" : "incoming"}`}>{!own ? <div className="direct-message-group-avatar"><DirectAvatar conversation={conversation} compact /></div> : null}<div className="direct-message-group-content">{!own ? <header className="direct-message-author"><strong><span>{conversation.participantName}</span><VerifiedBadge verification={verification} size="xs" /></strong><span>@{conversation.participantUsername}</span></header> : null}{group.messages.map((message) => <div className="direct-message-unit" key={message.id}>{message.replyPreview ? <button type="button" className="direct-reply-preview" title="Jump to replied message"><AppIcon name="reply" size="xs" /><span><strong>{message.replyPreview.authorName}</strong><small>{message.replyPreview.body || "Message unavailable"}</small></span></button> : null}{message.body || message.deletedAt ? <div className="direct-message-bubble">{message.deletedAt ? <em>Message deleted</em> : message.body}</div> : null}{!message.deletedAt && message.attachments?.length ? <DirectAttachmentGrid attachments={message.attachments} onPreview={onPreview} /> : null}{!message.deletedAt && message.reactions?.length ? <div className="direct-reaction-row">{message.reactions.map((reaction) => <button key={reaction.emoji} type="button" className={reaction.reactedByCurrentUser ? "active" : ""} aria-label={`${reaction.emoji} reaction, ${reaction.count}`}><span>{reaction.emoji}</span>{reaction.count}</button>)}</div> : null}{message.id === finalMessage.id ? <small className="direct-message-meta"><time dateTime={message.createdAt}>{dateTimeService.formatMessageTime(message.createdAt)}</time>{message.editedAt && !message.deletedAt ? <span>Edited</span> : null}{own && !message.deletedAt ? <span>Sent</span> : null}</small> : null}</div>)}</div></article>;
}

export function DirectMessageItem({ message, conversation, currentUserId }: { message: DirectMessage; conversation: DirectConversation; currentUserId: string }) {
  return <DirectMessageGroup group={{ id: message.id, authorId: message.authorId, messages: [message] }} conversation={conversation} currentUserId={currentUserId} onPreview={() => undefined} />;
}

export function DirectMessageList({ conversation, currentUserId, query, onPreview }: { conversation: DirectConversation; currentUserId: string; query: string; onPreview: (attachment: Attachment) => void }) {
  const endRef = useRef<HTMLDivElement | null>(null);
  const normalized = query.trim().toLocaleLowerCase();
  const messages = normalized ? conversation.messages.filter((message) => message.body.toLocaleLowerCase().includes(normalized)) : conversation.messages;
  useEffect(() => { if (!normalized) endRef.current?.scrollIntoView({ block: "end" }); }, [conversation.id, conversation.messages.length, normalized]);
  const groups = groupDirectMessages(messages);
  if (!messages.length) return <div className="direct-chat-empty"><AppIcon name={normalized ? "search" : "inbox"} size="xl" /><strong>{normalized ? "No matching messages" : "Start a direct message"}</strong><span>{normalized ? "Try a different search phrase." : "Messages here are visible only to conversation participants."}</span></div>;
  return <div className="direct-message-list" aria-live="polite"><div className="direct-message-stack">{groups.map((group) => <DirectMessageGroup key={group.id} group={group} conversation={conversation} currentUserId={currentUserId} onPreview={onPreview} />)}<div ref={endRef} /></div></div>;
}

export function DirectMessageComposer({ conversationId, onSendMessage }: { conversationId: string; onSendMessage: (conversationId: string, body: string) => Promise<boolean> }) {
  const [body, setBody] = useState(() => messageDraftService.getDraft({ directConversationId: conversationId })?.text ?? "");
  const [sending, setSending] = useState(false); const [error, setError] = useState<string | null>(null); const [emojiOpen, setEmojiOpen] = useState(false); const [attachmentNotice, setAttachmentNotice] = useState(false);
  useEffect(() => { setBody(messageDraftService.getDraft({ directConversationId: conversationId })?.text ?? ""); setEmojiOpen(false); setAttachmentNotice(false); }, [conversationId]);
  const updateBody = (value: string) => { setBody(value); setError(null); messageDraftService.setDraft({ directConversationId: conversationId }, value); };
  const submit = async (event: FormEvent) => { event.preventDefault(); const value = body.trim(); if (!value || sending) return; setSending(true); setError(null); const sent = await onSendMessage(conversationId, value); setSending(false); if (!sent) { setError("Message was not sent. Your draft is still here."); return; } messageDraftService.clearDraft({ directConversationId: conversationId }); setBody(""); };
  return <form className="direct-composer" onSubmit={(event) => void submit(event)} aria-busy={sending}><button type="button" aria-label="Attach an image" title="Attach image" onClick={() => setAttachmentNotice((current) => !current)}><AppIcon name="paperclip" size="sm" /></button><textarea value={body} disabled={sending} onChange={(event) => updateBody(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); event.currentTarget.form?.requestSubmit(); } }} placeholder="Message privately" rows={1} maxLength={4000} /><button type="button" aria-label="Choose emoji" title="Choose emoji" onClick={() => setEmojiOpen((current) => !current)}><AppIcon name="smile" size="sm" /></button><button type="submit" className="direct-send" aria-label="Send direct message" disabled={!body.trim() || sending}><AppIcon name="send" size="sm" /></button>{emojiOpen ? <div className="direct-emoji-picker" aria-label="Emoji choices">{["👍", "❤️", "😂", "🔥", "👀"].map((emoji) => <button key={emoji} type="button" aria-label={`Insert ${emoji}`} onClick={() => { updateBody(`${body}${body ? " " : ""}${emoji}`); setEmojiOpen(false); }}>{emoji}</button>)}</div> : null}{attachmentNotice ? <span className="direct-composer-note">Image upload will use the private attachment service.</span> : null}{error ? <span className="direct-composer-error" role="alert">{error}</span> : null}</form>;
}

export function DirectChatMain({ conversation, currentUserId, detailsOpen, onDetailsToggle, onBack, onOpenProfile, onPreview, onSendMessage }: { conversation?: DirectConversation; currentUserId: string; detailsOpen: boolean; onDetailsToggle: () => void; onBack: () => void; onOpenProfile: (userId: string) => void; onPreview: (attachment: Attachment) => void; onSendMessage: (conversationId: string, body: string) => Promise<boolean> }) {
  const [searchOpen, setSearchOpen] = useState(false); const [searchQuery, setSearchQuery] = useState("");
  useEffect(() => { setSearchOpen(false); setSearchQuery(""); }, [conversation?.id]);
  if (!conversation) return <main className="direct-chat-panel"><div className="empty-state"><AppIcon name="inbox" size="xl" /><strong>Select a conversation</strong><p>Choose a recent conversation or start a direct message from a profile.</p></div></main>;
  return <main className="direct-chat-panel"><DirectChatHeader conversation={conversation} searchOpen={searchOpen} searchQuery={searchQuery} detailsOpen={detailsOpen} onSearchOpen={() => { setSearchOpen((current) => !current); if (searchOpen) setSearchQuery(""); }} onSearchChange={setSearchQuery} onDetailsToggle={onDetailsToggle} onBack={onBack} onOpenProfile={onOpenProfile} /><DirectMessageList conversation={conversation} currentUserId={currentUserId} query={searchQuery} onPreview={onPreview} /><DirectMessageComposer conversationId={conversation.id} onSendMessage={onSendMessage} /></main>;
}

export function DMDetailsPanel({ conversation, onOpenProfile, onPreview, onClose, onSetMuted, onArchive }: { conversation?: DirectConversation; onOpenProfile: (userId: string) => void; onPreview: (attachment: Attachment) => void; onClose: () => void; onSetMuted: (conversationId: string, muted: boolean) => Promise<boolean>; onArchive: (conversationId: string) => Promise<boolean> }) {
  const [muted, setMuted] = useState(Boolean(conversation?.muted)); const [notice, setNotice] = useState<string | null>(null); const [savingPreference, setSavingPreference] = useState(false);
  useEffect(() => { setMuted(Boolean(conversation?.muted)); setNotice(null); }, [conversation?.id, conversation?.muted]);
  if (!conversation) return <aside className="dm-details-panel"><div className="direct-list-empty"><AppIcon name="user" size="xl" /><strong>Conversation details</strong><span>Select a conversation to view participant information.</span></div></aside>;
  const verification = getUserVerificationSummary(conversation.participantUserId);
  return <aside className="dm-details-panel" aria-label="Direct message details"><header className="dm-details-topbar"><div><span className="eyebrow">Conversation</span><strong>Details</strong></div><button type="button" className="icon-button dm-details-close" aria-label="Close conversation details" onClick={onClose}><AppIcon name="close" size="sm" /></button></header><section className="dm-profile-summary"><DirectAvatar conversation={conversation} large /><div><h2 className="dm-profile-name"><span>{conversation.participantName}</span><VerifiedBadge verification={verification} size="sm" /></h2><span>@{conversation.participantUsername}</span><small>{conversation.participantStatusText}</small></div><button type="button" onClick={() => onOpenProfile(conversation.participantUserId)}><AppIcon name="user" size="sm" />View profile</button></section><section className="dm-detail-section"><header><strong>Mutual communities</strong><span>{conversation.mutualCommunities?.length ?? 0}</span></header>{conversation.mutualCommunities?.length ? <div className="dm-mutual-list">{conversation.mutualCommunities.map((community) => <button type="button" key={community.id} onClick={() => setNotice(`${community.name} can be opened from the Server Rail.`)}><span>{community.name.slice(0, 1)}</span><strong>{community.name}</strong><AppIcon name="chevronRight" size="xs" /></button>)}</div> : <small>No shared communities.</small>}</section><section className="dm-detail-section"><header><strong>Shared media</strong><button type="button" className="dm-see-all" onClick={() => setNotice(`${conversation.sharedMedia?.length ?? 0} shared items in this conversation.`)}>See all</button></header>{conversation.sharedMedia?.length ? <div className="dm-shared-media">{conversation.sharedMedia.slice(0, 6).map((media) => media.type === "image" ? <button type="button" key={media.id} aria-label={`Preview ${media.name}`} onClick={() => onPreview(toPreviewAttachment(media))}><img src={media.url} alt={media.name} /></button> : null)}</div> : <small>No shared media yet.</small>}</section><section className="dm-detail-section dm-privacy-actions"><header><strong>Privacy and safety</strong><AppIcon name="lock" size="sm" /></header><button type="button" disabled={savingPreference} className={muted ? "active" : ""} onClick={() => { const next = !muted; setSavingPreference(true); void onSetMuted(conversation.id, next).then((saved) => { setSavingPreference(false); if (!saved) return; setMuted(next); setNotice(next ? "Conversation notifications muted." : "Conversation notifications restored."); }); }}><AppIcon name="bell" size="sm" />{muted ? "Unmute conversation" : "Mute conversation"}</button><button type="button" disabled={savingPreference} onClick={() => { setSavingPreference(true); void onArchive(conversation.id).then((saved) => { setSavingPreference(false); if (saved) setNotice("Conversation archived."); }); }}><AppIcon name="inbox" size="sm" />Archive conversation</button><button type="button" onClick={() => setNotice("Blocking is managed by Picom privacy controls.")}><AppIcon name="lock" size="sm" />Block user</button><button type="button" className="danger" onClick={() => setNotice("Reporting opens the centralized safety flow.")}><AppIcon name="more" size="sm" />Report user</button>{notice ? <p role="status">{notice}</p> : null}</section></aside>;
}

export function DirectMessagesView({ conversations, activeConversationId, currentUserId, friendRequestCount, onSelectConversation, onBackToCommunity, onOpenFriends, onOpenPendingFriends, onOpenProfile, onSendMessage, onSetMuted, onArchive }: DirectMessagesViewProps) {
  const activeConversation = conversations.find((conversation) => conversation.id === activeConversationId);
  const [detailsOpen, setDetailsOpen] = useState(false);
  const [previewAttachment, setPreviewAttachment] = useState<Attachment | null>(null);
  useEffect(() => { setDetailsOpen(false); }, [activeConversationId]);
  return <><section className={`direct-messages-view ${detailsOpen ? "details-open" : ""}`} aria-label="Direct messages"><DirectMessagesSidebar conversations={conversations} activeConversationId={activeConversationId} friendRequestCount={friendRequestCount} onSelectConversation={onSelectConversation} onOpenFriends={onOpenFriends} onOpenPendingFriends={onOpenPendingFriends} /><DirectChatMain conversation={activeConversation} currentUserId={currentUserId} detailsOpen={detailsOpen} onDetailsToggle={() => setDetailsOpen((open) => !open)} onBack={onBackToCommunity} onOpenProfile={onOpenProfile} onPreview={setPreviewAttachment} onSendMessage={onSendMessage} /><DMDetailsPanel conversation={activeConversation} onOpenProfile={onOpenProfile} onPreview={setPreviewAttachment} onClose={() => setDetailsOpen(false)} onSetMuted={onSetMuted} onArchive={onArchive} /></section>{previewAttachment ? <ImagePreviewModal image={previewAttachment} onClose={() => setPreviewAttachment(null)} /> : null}</>;
}
