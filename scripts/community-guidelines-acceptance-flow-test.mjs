import { readFileSync } from "node:fs";
const modal=readFileSync("src/components/CommunityMenu.tsx","utf8"),service=readFileSync("src/services/communityRulesService.ts","utf8"),membership=readFileSync("src/services/community/communityMembershipService.ts","utf8"),migration=readFileSync("supabase/migrations/20260710244000_community_guidelines_acceptance.sql","utf8"),docs=readFileSync("docs/community-guidelines-acceptance.md","utf8");
const checks=[[modal.includes("Community rules")&&modal.includes("rulesAccepted")&&modal.includes("Legal review marker"),"explicit join consent"],[service.includes("loadPublishedRules")&&service.includes("rulesVersion"),"versioned local acceptance"],[membership.includes("RULES_ACCEPTANCE_REQUIRED")&&membership.includes("accepted_rules_version"),"service gate"],[migration.includes("rules_accepted_at")&&migration.includes("rules_version_accepted")&&migration.includes("security definer"),"atomic server evidence"],[migration.includes("rules_enabled boolean not null default false")&&migration.includes("community_rules_owner_admin_manage"),"safe rollout and editor authorization"],[docs.includes("invite")&&docs.includes("legal")&&docs.includes("server timestamp"),"limitations documented"]];
const failed=checks.filter(([ok])=>!ok).map(([,label])=>label);if(failed.length)throw new Error(`Community guidelines acceptance failed: ${failed.join(", ")}`);console.log("Community guidelines join gate, timestamp, version, RLS, and safe rollout contracts passed.");
