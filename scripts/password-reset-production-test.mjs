import{readFileSync}from"node:fs";const auth=readFileSync("src/services/authService.ts","utf8"),login=readFileSync("src/components/LoginScreen.tsx","utf8"),links=readFileSync("src/services/deepLinkService.ts","utf8"),client=readFileSync("src/services/supabase/supabaseClient.ts","utf8"),app=readFileSync("src/App.tsx","utf8"),config=readFileSync("supabase/config.toml","utf8"),docs=readFileSync("docs/password-reset-production.md","utf8");const checks=[[auth.includes("PASSWORD_RESET_COOLDOWN_MS")&&auth.includes("getPasswordResetSafeMessage")&&auth.includes("resetPasswordForEmail"),"neutral rate-limited request"],[client.includes('flowType: "pkce"')&&links.includes('type: "passwordRecovery"')&&links.includes('url: "picom://auth/reset-password"'),"sanitized PKCE deep link"],[auth.includes("exchangeCodeForSession")&&auth.includes("updateUser({ password: newPassword })")&&auth.includes('signOut({ scope: "global" })'),"confirm and revoke sessions"],[login.includes('autoComplete="new-password"')&&login.includes("Passwords do not match")&&login.includes("at least 12 characters"),"confirm UI"],[app.includes("passwordRecoveryMode")&&app.includes("preparePasswordRecovery"),"app routing"],[config.includes("picom://auth/reset-password")&&docs.includes("SMTP")&&docs.includes("hosted staging"),"provider gate"],[!auth.includes("console.log")&&!links.includes("access_token")&&!links.includes("refresh_token"),"no token logging or fragment tokens"]];const failed=checks.filter(([ok])=>!ok).map(([,label])=>label);if(failed.length)throw new Error(`Password reset production failed: ${failed.join(", ")}`);console.log("Password reset request, enumeration safety, cooldown, PKCE confirm, deep link, and session revocation contracts passed.");
