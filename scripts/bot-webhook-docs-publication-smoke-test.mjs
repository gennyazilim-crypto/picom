import{readFile}from"node:fs/promises";const[d,b,w,a,p]=await Promise.all([readFile("docs/developer-portal/bot-webhook-beta-docs.md","utf8"),readFile("docs/bots/bot-api-v1.md","utf8"),readFile("docs/webhooks/webhook-production-hardening.md","utf8"),readFile("docs/api/public-api-versioning.md","utf8"),readFile("package.json","utf8")]);const c=[[d.includes("Not public and not an API launch")&&d.includes("non-runnable"),"status"],[d.includes("Authorization: Bot <one-time-token>")&&d.includes("Effective permission is intersection"),"bot auth"],[d.includes("X-Picom-Webhook-Token: <one-time-token>")&&d.includes("16 KiB")&&d.includes("30 requests/60 seconds"),"webhook"],[d.includes("RATE_LIMITED")&&d.includes("Idempotency-Key")&&d.includes("schema version"),"contracts"],[d.includes("No spam")&&d.includes("exclude token/header/hash/message body/private content"),"abuse privacy"],[b.includes("No public endpoint")&&w.includes("Remaining blockers")&&a.includes("does not publish or enable")&&p.includes('"developer:docs:bot-webhook:smoke"'),"gates"]];const forbidden=[/https:\/\/(?!<approved)[a-z0-9.-]+\//i,/picom_bot_[A-Za-z0-9_-]{10,}/,/Bearer\s+[A-Za-z0-9._-]{16,}/];for(const q of forbidden){if(q.test(d)){console.error(`FAIL: secret/host pattern ${q}`);process.exit(1)}}const f=c.filter(([o])=>!o);if(f.length){for(const[,l]of f)console.error(`FAIL: ${l}`);process.exit(1)}for(const[,l]of c)console.log(`PASS: ${l}`);
