import{readFile}from"node:fs/promises";const[p,r,j]=await Promise.all([readFile("docs/security/penetration-test-plan.md","utf8"),readFile("docs/security/pentest-report-template.md","utf8"),readFile("package.json","utf8")]);const c=[[p.includes("Production attack testing is prohibited")&&p.includes("isolated staging"),"authorization"],[p.includes("RLS/adversarial IDs")&&p.includes("preload IPC")&&p.includes("LiveKit token"),"coverage"],[p.includes("owner/admin/moderator/member/visitor")&&p.includes("synthetic"),"fixtures"],[p.includes("Stop immediately")&&p.includes("smallest synthetic record"),"stop rules"],[r.includes("Minimal reproducible steps")&&r.includes("Retest result")&&r.includes("Never include passwords"),"report"],[p.includes("not a penetration test result")&&j.includes('"security:pentest:prep:smoke"'),"no false claim"]];const f=c.filter(([o])=>!o);if(f.length){for(const[,l]of f)console.error(`FAIL: ${l}`);process.exit(1)}for(const[,l]of c)console.log(`PASS: ${l}`);
